services:
  betterfood:
    container_name: betterfood
    image: ghcr.io/pantelx/betterfood:latest
    env_file:
      - .env
    ports:
      - "3000:3000"
    volumes:
      - data:/app/data
    restart: unless-stopped

volumes:
  data:
